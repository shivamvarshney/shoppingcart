import { TitleinoDirective } from './titleino.directive';

describe('TitleinoDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleinoDirective();
    expect(directive).toBeTruthy();
  });
});
